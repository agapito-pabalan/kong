version: "3.7"
services:
  kong:
    build:
      context: .
    ports:
      - "8000:8000/tcp"
      - "8444:8444/tcp"
  konga:
    image: pantsel/konga:next
    restart: always
    environment:
      NODE_ENV: production
    init: true
    ports:
      - "1337:1337"
