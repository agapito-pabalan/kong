version: "3.7"
services:
  kong:
    image: gcr.io/stord-nonprod/kong:${CIRCLE_WORKFLOW_ID}
    build:
      context: .
      args:
        KONG_DATABASE: "off"
        KONG_DECLARATIVE_CONFIG: /usr/local/share/kong.yml
        KONG_ADMIN_LISTEN: 0.0.0.0:8001
        KONG_PROXY_LISTEN: 0.0.0.0:8000
        KONG_PLUGINS: bellatrix_bridge,cors,request-size-limiting,prometheus
        KONG_GO_PLUGINS_DIR: /usr/local/share/go-plugins
        KONG_PROXY_ACCESS_LOG: /dev/stdout
        KONG_PROXY_ERROR_LOG: /dev/stderr
        KONG_ADMIN_ACCESS_LOG: /dev/stdout
        KONG_ADMIN_ERROR_LOG: /dev/stderr
        KONG_LOG_LEVEL: info
        SSL_CERTIFICATE_1: $NONPROD_SSL_CERTIFICATE_1
        SSL_CERTIFICATE_2: $NONPROD_SSL_CERTIFICATE_2
        SSL_CERTIFICATE_PRIVATE_KEY: $NONPROD_SSL_CERTIFICATE_PRIVATE_KEY
        SNI_NAME: $NONPROD_SNI_NAME
    ports:
      - "8000:8000/tcp"
      - "8001:8001/tcp"